version: "3.11"

services:
  modem:
    build:
      context: .
      dockerfile: src/modem/Dockerfile
    ports:
      - "8000:8000"
      - "8001:8001"
      - "8002:8002"

  client:
    build:
      context: .
      dockerfile: src/client/Dockerfile
    stdin_open: true
    tty: true
